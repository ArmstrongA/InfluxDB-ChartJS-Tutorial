# InfluxDB-NodeJs-ChartJS
Visualizing InfluxDB time series data with chartjs

Run server with ```npm start```
Navigate to ```/public``` directory and run ```index.html``` in a live server to view charts
